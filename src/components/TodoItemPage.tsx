import React, { FC } from 'react';

interface IComponentProps {}

const TodoItemPageComponent: FC<IComponentProps> = () => {
  return (
    <div>
      <div>content</div>
    </div>
  );
};

export const TodoItemPage = TodoItemPageComponent;
